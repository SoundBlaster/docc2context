## Summary
- Added input detection for DocC bundles and archive files, returning helpful errors for unsupported archive inputs.
- Surfaced archive detection through the CLI so users are guided to extract `.doccarchive` files before conversion.

## Testing
- `swift test`
